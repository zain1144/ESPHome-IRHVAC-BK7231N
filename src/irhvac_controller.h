#pragma once

// The implementation stays at the repository root for easy source browsing.
// This wrapper exposes it as a normal PlatformIO library header, allowing an
// ESPHome YAML file to fetch it directly from GitHub.
#include "../irhvac_controller.h"

